import tw from 'twin.macro';
import styled from 'styled-components';

const Card = styled.article`
  ${tw`flex flex-col px-4 py-3 mx-auto bg-white border-2 rounded-sm shadow border-gray-50`}
`;

export default Card;
